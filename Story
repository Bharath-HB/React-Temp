Create Admin Event approval page using react and tailwind. "Profile" ,"Events" and "Approve" buttons (On sidebar) ,it shows a approval window in which all events status which are shown as pending is displayed there and corresponding to each event there is a "tick mark" and an cross mark as buttons to approve or reject the event( Center of the page) username and signout button in nav bar
